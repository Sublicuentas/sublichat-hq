{
  "name": "sublichat-activar-tv-cloudflare",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "engines": { "node": ">=22" },
  "scripts": {
    "test": "node --test test/*.test.js",
    "check": "wrangler deploy --dry-run --outdir dist",
    "deploy": "wrangler deploy"
  },
  "dependencies": { "@cloudflare/playwright": "1.3.0" },
  "devDependencies": { "wrangler": "4.130.0" }
}
